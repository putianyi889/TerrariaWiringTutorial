name: whitespace

on: 
  push:
    branches:
      - master
  pull_request:

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install latexindent
        run: |
          sudo apt-get update
          sudo apt-get install -y texlive-extra-utils

      - name: Check formatting
        run: |
          latexindent --GCString -sl -kv -r -l -wd -s -c -t /tmp $(git ls-files '*.tex')

      - name: Upload Log
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          path: indent.log